{% for row in rows %}
  {% set docente = row.content['#row']._entity %}
  {% set programas = row.content['#view'].style_plugin.getField(loop.index, 'title_1') %}

  <div>
    <h3>{{ docente.label }}</h3>
    <div>Programas: {{ programas }}</div>
  </div>
{% endfor %}