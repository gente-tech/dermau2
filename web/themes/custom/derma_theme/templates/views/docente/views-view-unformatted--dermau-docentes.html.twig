{% for row in rows %}
  {% set docente = row.content['#row']._entity %}
  {% set programa = row.content['#view'].style_plugin.getField(loop.index0, 'title_1') %}

  <div>
    <h3>{{ docente.label }}</h3>
    <div>Programa: {{ programa }}</div>
  </div>
{% endfor %}